# TASK-S5 — Hatena Blog AtomPub credentials. Copy this file to `.env` and
# fill in real values there. `.env` is gitignored — never commit real
# credentials. This file (.env.example) must never contain real values.
HATENA_ID=
HATENA_BLOG_ID=
HATENA_API_KEY=
